To run this program you need to install the pyaudio, SpeechRecognition, and pyttsx


You need to open comandline from the Python Program folder "scripts" and manually install the following:
	pip install pyaudio 

	pip install SpeechRecognition
	pip install pyttsx 